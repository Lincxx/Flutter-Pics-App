// I need to import a helper library from flutter
//Flutter
import 'package:flutter/material.dart';

//from lib into in lib
import 'src/app.dart';


// Define a 'main' function to run when our app starts
void main() {
    runApp(App());
}
